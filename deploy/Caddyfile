{$APP_DOMAIN:localhost} {
  encode zstd gzip
  handle /api/* { reverse_proxy api:3000 }
  handle /health { reverse_proxy api:3000 }
  handle /ready { reverse_proxy api:3000 }
  handle { reverse_proxy web:80 }
}
